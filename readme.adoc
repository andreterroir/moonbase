= RWAL

== Create a test block device

Create a 1MB file of zeroes:

[listing]
dd if=/dev/zero of=blockdev bs=1024 count=1024

Create a virtual block loop device backed by the file:

[listing]
losetup --show blockdev
